name: 01 - building Blocks
on: push
job:
    echo hello:
        runs-on: ubuntu-latest
        steps:
            -name: Say hello
             run: echo "hello world"
    echo goodbuy:
        runs-on: ubuntu-latest
        steps:
            -name: Failed step
             run: |
               echo "i will fail"
               exit 1
            -name : Say goodbye
             run: echo "goodbye!"